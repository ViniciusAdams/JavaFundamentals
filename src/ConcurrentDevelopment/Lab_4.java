package ConcurrentDevelopment;

import java.util.concurrent.locks.Lock;
import java.util.concurrent.locks.ReentrantLock;

public class Lab_4 {
private int num;
private Lock[] locked;
private boolean [] aseats;

public void Viewing (int num){
    this.num = num;
    this.locked = new ReentrantLock[num];
    this.aseats = new boolean[num];
for (int i = 0;  i < num ; i++){

}

//will update the final version by editing tomorrow .
}
}
